apiVersion: v1
kind: Service
metadata:
  name: node-express-app
  namespace: production
spec:
  type: NodePort
  selector:
    app: node-express-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080
